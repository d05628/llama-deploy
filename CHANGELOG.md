# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.6.0] - 2026-09-26

### 新增：agent 测试题组（`agent_bench/`）

固定的 4 道题，无人值守地测试各 CLI agent 并自动评分，换模型/量化/引擎/agent 后可重跑横向比较：
电影文件夹视频解读（找文件 + 视觉 + 语音转写）、Blender 火柴人 30 秒动画、鹈鹕骑自行车 SVG、
以及一道修 bug 题——在现成的多文件代码库里定位 3 个不同类型的根因、不许改测试、自行验证，
完全可客观评分，弥补前几题难以打分、也没考到"读懂已有代码并按规格修改"的盲区。
用法见 [agent_bench/README.md](agent_bench/README.md)。

每个工具用各自原生的权限机制只放行题目需要的操作，逐一实测过是否真的生效：
Claude Code 命令白名单 ✅、OpenCode 按命令规则 ✅、Codex 沙箱（只能写工作目录）✅；
**Qwen Code / Gemini CLI 的命令白名单在无人值守时不生效**（只放行 `ffprobe`，`whoami` 照样执行），
因此不在无人值守下给它们跑需要命令的题。

### 界面

- 「一键启动 Agent」按实测推荐排序并标注：Codex（推荐·最准）、OpenCode（推荐·最快）、Qwen Code（交互推荐）、
  Claude Code、Aider（只改代码）、Gemini CLI（备用）；简介写明实测表现与适用场景，并附"按用途选"速查。
- 「局域网与兼容网关」去掉 5 个只能复制命令的卡片（本机已可一键启动），改为一张接入地址表：
  OpenAI / Anthropic / Ollama / Gemini 兼容地址与直连地址，各一个复制按钮，供局域网内其他电脑或软件接入。
- 支持用 `#deploy` 这类地址直接打开对应页面。
- 模型服务运行时，推荐页不再给出"权重 ≤0.0GB 可驻留"的建议（此前把服务自己占的显存当成了不可用）。

### 修复

- **Codex 改不了任何文件**：Windows 上不配沙箱时 `workspace-write` 被强制降为只读
  （"read-only under a managed profile"），命令也被拒绝。配置里加 `[windows] sandbox = "unelevated"`
  （无需管理员提权），实测可写工作目录、写工作目录以外仍被拦。
- **Claude Code 遇到一次参数写错的工具调用就提前收工**：网关把修不好的调用（如缺 `file_path`）
  改写成一段说明文字，客户端当成"回答完毕"直接结束——实测 Blender 渲染报错后它本该改脚本，
  却因此放弃。现在原样交给客户端校验并把错误回给模型，模型改正后继续；同一题由 1/4 变为 4/4。
- **Gemini CLI 报 "Incomplete JSON segment at the end"**：它的 SSE 解析器要求每段以 `data: ` 开头，
  1.5.0 加的注释行心跳会卡在缓冲区里把后续数据全部堵住。Gemini 路径的心跳改为只带用量统计的 `data` 行。

## [1.5.2] - 2026-09-25

用户实测 1.5.1 各 agent 后的第二轮修复。

### 修复

- **Codex 完全无法启动**：Codex 0.157 默认启动共享后台服务，在管理员窗口里直接拒绝运行
  （"start the Windows daemon from a non-elevated terminal"）。启动器加 `--no-daemon`。
- **Qwen Code、OpenCode 说自己"没有视觉能力"**：两者对未知模型名一律按纯文本处理，不声明就不把图片
  发给模型。开「看图」时在各自的模型配置里显式声明（Qwen Code `generationConfig.modalities.image`，
  OpenCode `attachment` + `modalities.input`，Aider `supports_vision`）。实测两者都能读出测试图里的数字和颜色。
- **工作目录仍是整个用户目录**：旧版界面把用户目录存进了浏览器，新默认目录不生效。
  遇到旧值（用户目录或盘符根目录）自动换成专用工作目录。
- **Aider 每一步都要确认**：改文件改为自动确认（每次改动都会 git 提交，`/undo` 可撤销）；
  同时关闭它的"建议执行命令"，避免自动确认连带执行 shell 命令。
- Gemini CLI 简介补充：若弹出登录选项，选「Use Gemini API Key」即可，无需登录 Google。

## [1.5.1] - 2026-09-25

修复用户实测 1.5.0「一键启动 Agent」时遇到的一连串问题。

### 修复

- **Qwen Code 报"上下文过大"**：它固定预留 2 万摘要输出 + 1.3 万缓冲，47104 的窗口
  1.4 万就开始压缩、2.4 万即硬上限，而它的系统提示 + 工具定义 + 技能就有约 2.8 万。
  一键启动现在自动切到 **agent 模式**（`run.py server --agent`：ngram 推测解码 + q4 KV +
  自动上下文），16GB 卡上约 **8.8 万**上下文；不改 `config.jsonc`，聊天仍用原配置。
  已在运行的服务不是 agent 模式时自动切换。
- **OpenCode 毫无反应**：默认工作目录是整个用户目录，它一直在给整个目录做 git 快照。
  默认工作目录改为专用的 `~/llama-agent-workspace`，选用户目录或盘符根目录时给出提示；
  OpenCode 关闭快照。
- **Qwen Code 闪退**（`write UNKNOWN`）：启动器切到 UTF-8 代码页（`chcp 65001`）时，
  Windows 10 旧版控制台下全屏终端界面写屏失败。启动器改为系统代码页保存、不再切换。
  同时修正启动器每行多一个回车（`\r\r\n`）。
- **「看图」默认开启**：操作桌面软件（如画图）需要看截图。视觉模块放 CPU，不占显存、不缩上下文。
- **装完 Aider 仍显示未安装**：`aider-install` 装到 `~/.local/bin`，已运行的管理器看不到新 PATH；
  现同时查找常见安装位置。实测 Aider 可用本地模型完成编辑。
- **设置页保存会悄悄改掉性能档位**：下拉框没有 `speed` 等当前档位名，读入后为空，
  保存时被写回 `auto`（即平衡档、MTP 关闭）。下拉框更新为当前档位名，旧名自动映射。

### 新增

- 设置页「使用场景」预设：日常对话 / 长对话长文档 / 超长上下文 / 质量优先，
  每项标明参考上下文与速度，点选即填好底层参数。
- 「关闭 agent 窗口并释放显存」：按启动器命令行识别本工具打开的窗口
  （Qwen Code 等会改写窗口标题），连同进程树关闭，并停止模型服务。
- 每个 agent 工具附一句简介。

## [1.5.0] - 2026-09-25

修复"重开后吐字从满速掉到 1/3"：根因是有层被挤到 CPU，而 llama.cpp 全程不提示。
实测数据见 [性能调优日志](docs/performance-tuning.md) 第 10 节。

### 新增

- **`ctx_size: 0` 自动上下文（新默认）**。在权重全部驻留 GPU 的前提下取显存允许的最大
  上下文，缩到 `performance.min_ctx_size`（默认 8192）仍装不下才卸层。
  IQ4_XS 在 16GB 卡上：原 `ctx 65536` 只放下 62/66 层、12.7 t/s；自动上下文取
  51456、66/66 层、**27.0 t/s**。纯 CPU 后端按 `min_ctx_size` 处理。

### 修复

- **显存预算按真正进显存的权重计算。** 此前用文件体积，把始终留在 CPU 的 `token_embd`
  和未启用时不加载的 MTP 层都算了进去（NVFP4：16332 vs 实际 15098 MiB）。
  诊断现在给出具体差多少 MiB。
- **权重装不下时不再启用 MTP。** 实测此时开 MTP 更慢（12.8 → 7.0 t/s，12.7 → 5.0 t/s）。
- **MTP 显存估算**计入混合架构每个草稿 token 的循环状态拷贝（约 155 MiB/个）。
- **停止服务后等进程真正退出**再返回，避免立刻重启时少算空闲显存；启动时检测残留的
  llama-server 并警告。

### 引擎更新：一键适配最新环境，保留回退

- **CUDA 包按主版本匹配驱动。** 此前要求包版本 ≤ 驱动报告的 CUDA 版本，驱动 581.80（CUDA 13.0）
  会错过 cuda-13.4 包、退回不含 sm_120 的 12.4。CUDA 次版本兼容保证同主版本可运行，已实测。
  RTX 50 上 NVFP4 提示词处理因此 **+44%**、吐字 +10%；K-quant 基本持平（+1~2%）。
- **升级验证要求新引擎能识别 GPU**（`--list-devices`），否则视为失败并自动回滚。
- **升级成功后保留上一版本**为 `llama.cpp.previous`，新增 `python deploy.py --rollback-llama`
  在两版之间互换。
- 管理器新增「推理引擎」卡片：当前版本与 CUDA 变体、最新版本、可回退版本，一键升级 / 一键回退。

### 一键启动 Agent（本地模型）

管理器「部署」页新增「一键启动 Agent」：自动启动模型服务与兼容网关，读取实际上下文长度，
在新窗口打开所选工具。支持 **Qwen Code（推荐）、Claude Code、Codex、OpenCode、Gemini CLI、Aider**。

- **不影响官方通道**：环境变量只写进生成的启动器（`agents/launch-*.cmd`），不改系统环境变量；
  每个工具使用 `agents/<id>/` 下独立的配置目录（`CLAUDE_CONFIG_DIR` / `CODEX_HOME` / `QWEN_HOME` /
  `GEMINI_CLI_HOME` / `XDG_*`），官方的登录、配置和会话历史互不混用。实测前后官方配置文件零改动。
- 按实际上下文告诉各工具真实窗口（`CLAUDE_CODE_MAX_CONTEXT_TOKENS`、Codex `model_context_window`、
  Qwen Code `contextWindowSize` 等），自动压缩会在撑爆前触发。
- 可选「需要看图」：视觉模块放 CPU，不占显存、不缩上下文。
- 自动把本机最新版 Blender 加入该窗口的 PATH。
- 实测 5 个已安装工具都能通过本地模型完成"用工具写文件"的任务。

### 兼容网关修复（均由上面的端到端测试发现）

- **流式 chat completions 返回 502**：网关把上游 SSE 当 JSON 解析。Qwen Code、OpenCode 等流式客户端
  全部不可用。现原样透传。
- **Responses 流缺少事件、工具调用丢失**：Codex 报 `OutputTextDelta without active item`。
  现按协议完整回放 `output_item.added` / `content_part` / `function_call_arguments` 等事件。
  Responses 未指定 `max_output_tokens` 时不再默认截断在 1024。
- **Gemini 工具调用失败**：流式路径丢弃工具调用；新版 Gemini CLI 用 `parametersJsonSchema`
  声明参数，网关只读 `parameters`，模型看不到参数定义。
- **工具调用历史被压平成文本**（Anthropic / Responses / Gemini 三条路径）：模型看到的是
  `[tool_use ...]` 文字记录，多步任务里会模仿文本而不真正调用工具。现转成真实的
  `tool_calls` / `tool` 消息。
- **图片被丢弃**：四种协议的图片现在都能透传给开了视觉的模型服务；工具读回的图片
  （如 agent 查看截图）也能送达。未开视觉时换成提示文字，不再把 base64 塞进提示词。
- **长回复无限重试**：网关对 Anthropic / Responses / Gemini 工具请求是先等上游完整生成再回放，
  agent 一次写出整个脚本要 2-3 分钟，其间一个字节都不发，客户端判定连接空闲、断开并原样重发——
  实测 Claude Code 把同一请求重发几十次后报 "Request timed out"。现在立即发出开始事件，
  等待期间每 5 秒一次心跳（Anthropic `ping` 事件 / SSE 注释行）。
- **网关 PID 被复用时误判**：重启后旧 PID 落到了 `conhost.exe`，`start` 误报"已在运行"，
  `stop` 会直接杀掉无关进程。现核对进程名，过期 PID 只清理文件、绝不 kill。
- Claude Code 启动器设置 `CLAUDE_CODE_GLOB_TIMEOUT_SECONDS=600`：默认 20 秒，
  实测 20 万目录的数据盘全盘搜索要 6–8 分钟。

### 调整

- `spec_draft_n_max` 默认 3 → 2：上下文多约 47%（15104 → 22272），散文速度不变，
  代码 63 → 55 t/s。

---

## [1.4.0] - 2026-09-05

性能档位化。在**不牺牲输出保真度**的前提下，代码场景吐字速度从 16.3 提升到
26.3 t/s（相对最初的 6.39 为 **4.1 倍**）；愿意接受输出变化时可达 34–56 t/s。

### 先验证了"推测解码是否无损"

推测解码理论上分布保持，但"理论上"不等于"这个实现里"。用贪心解码逐字节比对，
并先做对照排除浮点噪声（同配置跑两次 3/3 一致、只改 `-ub` 3/3 一致、
MTP 自身两次运行 3/3 一致，证明实现是确定性的）：

| 组件 | 与基线贪心输出比对 | 结论 |
|---|---|---|
| `ngram-map-k` | **3/3 完全一致** | 无损 |
| `draft-mtp` | **0/3 一致** | 会改变输出 |

**`draft-mtp` 的 1.4–2.1 倍提速是拿输出保真度换的。** 这不等于"更差"，
但既然无法证明不更差，就不应该默认打开。

### 新增：quality / balanced / speed 三档

| 档位 | KV | 推测解码 | 代码场景实测 | 输出保真 |
|---|---|---|---:|---|
| `quality` 质量 | f16 | 无 | 26.4 t/s | 与参考解码逐字节一致 |
| `balanced` 平衡（默认） | q8_0 | 无 | 26.3 t/s | 与参考解码逐字节一致 |
| `speed` 极速 | q8_0 | draft-mtp + ngram | 34–56 t/s | **会与参考解码不同** |

- 质量档与平衡档**速度几乎相同**——权重完整驻留后 KV 精度不再影响吐字速度，
  只影响显存占用，平衡档省下的显存可用于更长上下文。
- 旧档位名 `auto` / `maximum` 对齐到 `balanced`，**不会因为改名而悄悄改变输出**；
  启用 MTP 必须显式选 `speed`，且启动时会明确警告保真度代价。
- `spec_type: "auto"` 表示跟随档位。

### 修复

- **视觉模式的显存预留过头了。** 此前按 `mmproj×1.1+512` 预留 1488MiB，
  实测在两个维度上都吃亏：预留过多把权重挤到 CPU，反过来连视觉编码本身也拖慢。
  收到 `mmproj×0.9`（768MiB）后，**文本快 69%（10.5→17.8 t/s）、
  单张图还快 24%（9.9→7.5s）**。

- 视觉模式不再一刀切禁用 MTP。实测 mmproj 放 CPU 时可正常配合推测解码。

### 新增：`performance.mmproj_offload`

控制视觉编码器放 GPU 还是 CPU。实测（896×896 图片，缓存未命中）：

| 配置 | 文本生成 | 单张新图端到端 |
|---|---:|---:|
| `true`（默认，放 GPU） | 17.8 t/s | 7.5 s |
| `false`（放 CPU） | 26.2 t/s | 29.0 s |

以文本为主、偶尔看图的用法适合设 `false`（806 个图片 token 在 CPU 上要跑 26 秒，
但文本生成回到满速）；图片密集的用法应保持 `true`。

### 测试

新增 7 个用例，覆盖三档的 KV 精度与显存余量、旧档位名兼容、未知档位回退，
以及视觉预留不再按"必须覆盖 mmproj 体积"的错误假设计算。共 **61 passed**。

---

## [1.3.1] - 2026-09-05

### 修复

- **视觉模式的显存开销此前完全不参与预算计算。** 开启视觉要常驻 `mmproj`
  （27B 的 BF16 视觉模块是 888MiB），且 `fit_target` 会从 64MiB 抬到
  `mmproj×1.1+512`＝1488MiB，两项合计约 **2376MiB**。这足以把本来刚好装得下的
  权重重新挤回 CPU —— 实测同一模型同一配置，文本模式 25.4 t/s，
  视觉模式只有 4–5 t/s，**相差六倍**，而 v1.2.0 加的显存诊断对此毫无提示。

  现在预算计算会计入 mmproj 与视觉模式的预留，并在启动时说清代价：

  > 视觉模式额外占用约 2376MiB 显存（视觉模块 888MiB + 预留 1488MiB；不需要图片理解时
  > 改用普通模式启动可显著提速）；实测该模型在普通模式下可完整驻留显存，
  > 开视觉会把约 1537MiB 权重挤回 CPU

  同时修正了预算口径：`fit_target` 是 llama.cpp 保留不用的显存，
  此前未从可用量中扣除，导致临界情况下判断偏乐观。

### 文档

- [docs/performance-tuning.md](docs/performance-tuning.md) 补入换档后的实测结果
  （**UD-IQ4_XS 达 25.4 t/s，相对起点 6.39 提升 4.0 倍**，略高于此前 20–24 的外推值），
  并新增第 8 节记录视觉模式的显存代价。

### 测试

新增 3 个用例：视觉模式必须报告其显存代价且给出可执行建议、视觉模式预留必须大于
文本模式、显式配置的 `fit_target_mb` 在视觉模式下仍然优先。共 **57 passed**。

---

## [1.3.0] - 2026-09-04

安全加固与界面修复。修掉 [docs/code-audit.md](docs/code-audit.md) 全量审查列出的 16 项问题。

### 安全

- **compat 网关的 `api_key` 此前从未被校验。** 它在 compat.py 里只出现一次
  （从配置读出来那行），`do_GET` / `do_POST` 没有任何鉴权分支，而网关默认监听
  `0.0.0.0:11434` —— 局域网内任何设备都能直接调用模型。更糟的是配置里写着
  `"api_key": "local-no-key-needed"`，会让人以为改成真 key 就受保护了。

  现已实现校验，支持 `Authorization: Bearer`、`X-Api-Key`、`api-key` 三种写法。
  `""` / `local-no-key-needed` / `none` / `disabled` / `no-key` 视为**不启用鉴权**，
  因此现有配置行为不变；设成任何其它值即刻生效。未启用鉴权且监听非回环地址时，
  启动日志会明确警告。健康探测端点保持开放，管理器仍能轮询状态。

- **管理器此前对任意网页开放。** 它默认绑 `127.0.0.1`，但对所有响应发
  `Access-Control-Allow-Origin: *` 且无鉴权。浏览器允许网页向 localhost 发跨域
  请求，通配 CORS 又明确授权对方**读取响应**，因此访问任意网站都可能被其脚本
  读取配置（含网关 key）、改配置、删模型、启停进程。

  现已移除通配 CORS（同源页面本就不需要），并加入三重校验：每次启动生成的
  一次性会话令牌（只随页面本体下发）、`Origin` / `Sec-Fetch-Site` 同源校验、
  以及 `Host` 头白名单（防 DNS rebinding）。

- **删除接口可用 glob 通配符删模型。** 只挡了 `..` `/` `\`，而实现是
  `MODELS_DIR.rglob(filename)`，传 `*.gguf` 会匹配到模型并删除。
  现改为逐个比对文件名，并校验解析后的真实路径仍在 `models/` 内（防符号链接逃逸）。

- **解压未校验成员路径（zip slip）。** 压缩包正常来自 GitHub Release，但
  `download.github_mirror` 是用户可配置的第三方镜像，带 `../` 条目的恶意包可以
  写到项目目录之外。现逐条校验路径并拒绝符号链接条目。

- **下载完整性校验 fail-open。** Release feed 没提供 digest 时直接放行，
  且与"校验通过"返回同一个值。现在会明确提示跳过了校验。

- **`pid_running()` 不校验进程身份。** PID 会被系统回收复用，只判断"存在这个
  PID"就执行 `taskkill /F` 会误杀无关进程。现在同时核对进程名，
  `run.py` 与 `manager.py` 的停止流程都会先确认目标确实是 llama-server。

- **前端无 HTML 转义。** 20 处 `innerHTML` 直接拼接模型源返回的
  `id` / `author` / `filename` / `tags`。原有的 `escapedId` 只处理了 JS 字符串
  上下文的引号，HTML 层面未转义。HuggingFace 官方 id 字符集受限，但管理器同时
  支持 ModelScope / Gitee / 用户可配置镜像，恶意或被劫持的源可借此注入脚本。
  现新增 `esc()` / `escArg()` 并应用到全部外部数据渲染点。

### 修复

- **配置默认值曾有三份副本**（Python `default_config()`、前端 `defaultCfg()`、
  `config.example.jsonc`），无任何同步机制。前端改为向新增的
  `/api/config/default` 拉取，示例文件由测试锁定，防止再次漂移。

- **`api()` 从不检查 HTTP 状态码。** `r.ok` 未判断，服务端返回 404 / 500 时
  只要 body 是合法 JSON 就会被当成正常数据渲染。现在会返回结构化错误，
  403 还会提示刷新页面重取令牌。

- **`activate()` 不校验模型文件是否存在**，可把 `model_file` 写成任意字符串，
  直到启动服务才报错。

- 3 处裸 `except:` 改为具体异常类型（裸 `except` 会连 `KeyboardInterrupt` 一起吞掉）。

- 启动横幅的 ASCII 边框会随 host 长度和 emoji 宽度错位，改为按显示宽度对齐。

### 界面

- **响应式基本缺失** —— 3300 行界面只有一个 768px 断点。更糟的是其中
  `.sys-info{display:none}` 在窄屏下**把设备信息整块藏掉**，而那里正是显存预算
  这类最该看的内容。现新增 1024 / 768 / 560px 三档断点，窄屏保留设备信息，
  手机竖屏改为顶部横向导航，宽表格独立横向滚动而不再撑破页面。

- **25 个 `<label>` 全部没有 `for=`**，点击标签不聚焦输入框，读屏软件也无法关联。
  现已全部绑定；导航补 `role="tablist"` / `aria-selected` 并支持键盘激活，
  提示框加 `role="status"` / `aria-live`。

- **14 处阻塞式 `alert()`** 与项目已有的 `showToast()` 并存，风格不一且打断操作流。
  现统一为 toast，失败类提示延长停留时间；破坏性操作的 `confirm()` 保留。

- 状态轮询定时器此前从不清理，页面切到后台仍持续请求。现在按
  `visibilitychange` 暂停与恢复。

### 测试

新增 15 个用例，覆盖路径与 glob 校验、zip slip 拦截、compat 鉴权判定、
PID 身份校验、配置默认值一致性，以及前端卫生检查（转义函数存在、
`api()` 检查状态码、无阻塞弹窗、label 全部绑定、窄屏不隐藏设备信息）。共 **54 passed**。

安全修复另经实机端到端验证：无令牌请求、跨站 `Origin`、`Sec-Fetch-Site: cross-site`、
伪造 `Host`、glob 删除、激活不存在的模型 —— 全部按预期拒绝。

---

## [1.2.1] - 2026-09-04

修复 Web 管理器中一批会给出错误建议的显示逻辑。这些问题会让用户选到装不下显存的
量化档位 —— 也正是 1.2.0 所修复的性能问题的源头。

### 修复

- **"✅ 兼容"判定把模型大小和显存总量比较**，而不是和扣除 KV cache、计算缓冲后的
  实际预算比较。15.9GB 的卡会把 15.3GB 的权重标成"兼容"，而它实际装不下。
  现在按权重预算判定，并区分三种状态：可完整驻留 / 超出多少（部分层落 CPU）/ 远超预算。

- **16GB 显卡被当成 8GB 档**。GPU 指南用 `(vram_mb/1024).toFixed(1) >= 16` 判档，
  而显卡实际上报 16311MiB＝15.9GB，这个条件**永远不成立**。改为直接用 MB 判档。

- **`BF16` 文件被标成 `F16`**。量化类型按"首个子串命中即停"匹配，而 `F16` 排在
  `BF16` 前面，`"bf16"` 里含有 `"f16"`。改为按名字长度从长到短匹配。

- **多种量化档位显示为 "unknown"**：`Q3_K_XL`、`Q4_K_XL`、`Q8_K_XL`、`IQ3_S`、
  `IQ2_S`、`MXFP4_MOE` 等都不在识别列表里。同一份识别逻辑在代码里有
  **4 份互不一致的副本**，其中本地模型库那份连 `IQ4_XS` 都没有。
  现已统一为单个 `detect_quant()`，覆盖 llama.cpp 支持的全部类型及 Unsloth UD 变体。

- **视觉模块显示主模型的量化**。配对命名会把主模型量化写进文件名
  （`...-Q4_K_M-mmproj-BF16.gguf`），现在只取 `mmproj` 之后的部分。

- **设备建议自相矛盾**。`get_recommendations()` 在显存 ≥6GB 时一律提示
  "显存充足，可使用 Q5_K_M 或 Q8_0 获得更好质量"，却完全不看模型多大 ——
  27B 的 Q5_K_M 是 19GB。同时 `推荐 ctx` 只根据**系统内存**计算（固定 2048），
  与真正的约束（显存）无关。现在两者都基于显存预算。

- **`gpu_layers=-1` 的说明是错的**：提示写"将全部层卸载到 GPU"，而它实际是
  按实时显存自动拟合层数；模型装不下时根本做不到全部卸载。

### 新增

- `manager.gpu_weight_budget_gb()` —— 权重能完整驻留显存的体积上限。
  按 1.2.0 的实测数据校准（16GB 卡实测预算 13.45GB）。
- `manager.detect_quant()` —— 统一的量化类型识别。
- 量化提示表补全所有类型，并以实测困惑度替换原先的主观描述
  （如 `IQ4_XS` 原写"比 Q4_K_S 小10%"，未提质量；现为"体积比 Q4_K_M 小约 12%，
  质量几乎持平（+3.1%）；显存吃紧时的首选"）。

### 测试

新增 11 个用例，覆盖量化识别（BF16/UD 变体/mmproj/前缀歧义）、显存预算边界
（必须判定 Q4_K_M 装不下而 IQ4_XS 装得下）、以及建议不再自相矛盾。共 37 passed。

内嵌前端 JS 现经 `node --check` 语法校验。

---

## [1.2.0] - 2026-09-03

围绕"显存装不下模型"这一场景的性能优化。在 RTX 5060 Ti 16GB + Qwen3.8-27B
（`qwen35` 混合注意力架构）上，吐字速度 **6.39 → 16.34 token/s（2.56×）**。

完整测量数据、方法论与无效路径记录见 [docs/performance-tuning.md](docs/performance-tuning.md)。

### 新增

- **显存预算诊断**。权重超出可用显存时，llama.cpp 会静默把层卸载到 CPU，
  速度掉数倍却毫无提示。现在启动时直接指出瓶颈，并给出可执行的建议：
  > 权重 15.3GB 超出可用显存 14.6GB，必然有一部分层留在 CPU 上，这是吐字慢的主因；
  > 当前 ctx_size=16384 还要额外占用 288MiB KV cache…若要让权重完整驻留显存，
  > 需换用体积 ≤13.8GB 的量化档位

  权重装得下、但上下文过长时，则给出当前显存下建议的 `ctx_size` 上限。

- `run.kv_cache_mb()` —— 按模型元数据估算 KV cache 显存占用。
  正确处理**混合注意力架构**：`full_attention_interval=4` 的模型每 4 层才有 1 层
  带 KV，其余是常数大小的 SSM 循环状态。按全层计算会把 KV **高估 4 倍**，
  进而误判上下文预算。

- `run.max_ctx_for_vram()` —— 反解当前显存下权重仍能完整驻留的最大上下文长度。

### 变更

- `gguf_meta()` 新增读取注意力相关字段（`head_count_kv` / `key_length` /
  `value_length` / `full_attention_interval` / `nextn_predict_layers`），
  改为按后缀表统一提取。此前只读 `block_count` 与 `context_length`，
  无法估算 KV cache。

- 27B / 16GB 高压档的 `--fit-target` 由 256 MiB 收紧到 64 MiB（`maximum` 档）。
  权重比显存大时，留给驱动的余量每多一点就少一点权重驻留 GPU；
  实测提速约 15%。`auto` 档保持 256 MiB 的保守值。

### 文档

- 新增 [docs/performance-tuning.md](docs/performance-tuning.md)：完整调优日志，
  含各档位实测数据、量化档位质量对比（困惑度实测）、复现方法，
  以及**已验证无效的优化路径**（见下）。

### 已验证无效（不要重复尝试）

这些手段看起来合理，实测均为负收益，细节见性能文档第 4 节：

| 手段 | 结果 |
|---|---|
| `-ot token_embd.weight=CPU` | 12.91 → **8.41** t/s，会干扰 `--fit` 的张量级自动放置 |
| `--spec-type draft-mtp` | 15.98 → **11.90** t/s，多占约 350MiB 显存把权重挤到 CPU |
| `--spec-type ngram-*` | +2%~6%，在噪声内 |
| 手工指定 `-ngl` | 均劣于 `-ngl auto --fit on` |

> 调优过程中曾单次测到 21.27 t/s（+45%），中位数复测后未能复现，属异常值。
> 本机单次测量波动可达 ±30%，所有结论均改用 3 次取中位数。

### 测试

新增 4 个用例，覆盖 KV 估算（含混合注意力与元数据缺失）与两类显存预警分支。

---

## [1.1.1] 及更早

见 git 提交历史。
