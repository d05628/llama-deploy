# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-09-04

安全加固与界面修复。修掉 [docs/code-audit.md](docs/code-audit.md) 全量审查列出的 16 项问题。

### 安全

- **compat 网关的 `api_key` 此前从未被校验。** 它在 compat.py 里只出现一次
  （从配置读出来那行），`do_GET` / `do_POST` 没有任何鉴权分支，而网关默认监听
  `0.0.0.0:11434` —— 局域网内任何设备都能直接调用模型。更糟的是配置里写着
  `"api_key": "local-no-key-needed"`，会让人以为改成真 key 就受保护了。

  现已实现校验，支持 `Authorization: Bearer`、`X-Api-Key`、`api-key` 三种写法。
  `""` / `local-no-key-needed` / `none` / `disabled` / `no-key` 视为**不启用鉴权**，
  因此现有配置行为不变；设成任何其它值即刻生效。未启用鉴权且监听非回环地址时，
  启动日志会明确警告。健康探测端点保持开放，管理器仍能轮询状态。

- **管理器此前对任意网页开放。** 它默认绑 `127.0.0.1`，但对所有响应发
  `Access-Control-Allow-Origin: *` 且无鉴权。浏览器允许网页向 localhost 发跨域
  请求，通配 CORS 又明确授权对方**读取响应**，因此访问任意网站都可能被其脚本
  读取配置（含网关 key）、改配置、删模型、启停进程。

  现已移除通配 CORS（同源页面本就不需要），并加入三重校验：每次启动生成的
  一次性会话令牌（只随页面本体下发）、`Origin` / `Sec-Fetch-Site` 同源校验、
  以及 `Host` 头白名单（防 DNS rebinding）。

- **删除接口可用 glob 通配符删模型。** 只挡了 `..` `/` `\`，而实现是
  `MODELS_DIR.rglob(filename)`，传 `*.gguf` 会匹配到模型并删除。
  现改为逐个比对文件名，并校验解析后的真实路径仍在 `models/` 内（防符号链接逃逸）。

- **解压未校验成员路径（zip slip）。** 压缩包正常来自 GitHub Release，但
  `download.github_mirror` 是用户可配置的第三方镜像，带 `../` 条目的恶意包可以
  写到项目目录之外。现逐条校验路径并拒绝符号链接条目。

- **下载完整性校验 fail-open。** Release feed 没提供 digest 时直接放行，
  且与"校验通过"返回同一个值。现在会明确提示跳过了校验。

- **`pid_running()` 不校验进程身份。** PID 会被系统回收复用，只判断"存在这个
  PID"就执行 `taskkill /F` 会误杀无关进程。现在同时核对进程名，
  `run.py` 与 `manager.py` 的停止流程都会先确认目标确实是 llama-server。

- **前端无 HTML 转义。** 20 处 `innerHTML` 直接拼接模型源返回的
  `id` / `author` / `filename` / `tags`。原有的 `escapedId` 只处理了 JS 字符串
  上下文的引号，HTML 层面未转义。HuggingFace 官方 id 字符集受限，但管理器同时
  支持 ModelScope / Gitee / 用户可配置镜像，恶意或被劫持的源可借此注入脚本。
  现新增 `esc()` / `escArg()` 并应用到全部外部数据渲染点。

### 修复

- **配置默认值曾有三份副本**（Python `default_config()`、前端 `defaultCfg()`、
  `config.example.jsonc`），无任何同步机制。前端改为向新增的
  `/api/config/default` 拉取，示例文件由测试锁定，防止再次漂移。

- **`api()` 从不检查 HTTP 状态码。** `r.ok` 未判断，服务端返回 404 / 500 时
  只要 body 是合法 JSON 就会被当成正常数据渲染。现在会返回结构化错误，
  403 还会提示刷新页面重取令牌。

- **`activate()` 不校验模型文件是否存在**，可把 `model_file` 写成任意字符串，
  直到启动服务才报错。

- 3 处裸 `except:` 改为具体异常类型（裸 `except` 会连 `KeyboardInterrupt` 一起吞掉）。

- 启动横幅的 ASCII 边框会随 host 长度和 emoji 宽度错位，改为按显示宽度对齐。

### 界面

- **响应式基本缺失** —— 3300 行界面只有一个 768px 断点。更糟的是其中
  `.sys-info{display:none}` 在窄屏下**把设备信息整块藏掉**，而那里正是显存预算
  这类最该看的内容。现新增 1024 / 768 / 560px 三档断点，窄屏保留设备信息，
  手机竖屏改为顶部横向导航，宽表格独立横向滚动而不再撑破页面。

- **25 个 `<label>` 全部没有 `for=`**，点击标签不聚焦输入框，读屏软件也无法关联。
  现已全部绑定；导航补 `role="tablist"` / `aria-selected` 并支持键盘激活，
  提示框加 `role="status"` / `aria-live`。

- **14 处阻塞式 `alert()`** 与项目已有的 `showToast()` 并存，风格不一且打断操作流。
  现统一为 toast，失败类提示延长停留时间；破坏性操作的 `confirm()` 保留。

- 状态轮询定时器此前从不清理，页面切到后台仍持续请求。现在按
  `visibilitychange` 暂停与恢复。

### 测试

新增 15 个用例，覆盖路径与 glob 校验、zip slip 拦截、compat 鉴权判定、
PID 身份校验、配置默认值一致性，以及前端卫生检查（转义函数存在、
`api()` 检查状态码、无阻塞弹窗、label 全部绑定、窄屏不隐藏设备信息）。共 **54 passed**。

安全修复另经实机端到端验证：无令牌请求、跨站 `Origin`、`Sec-Fetch-Site: cross-site`、
伪造 `Host`、glob 删除、激活不存在的模型 —— 全部按预期拒绝。

---

## [1.2.1] - 2026-09-04

修复 Web 管理器中一批会给出错误建议的显示逻辑。这些问题会让用户选到装不下显存的
量化档位 —— 也正是 1.2.0 所修复的性能问题的源头。

### 修复

- **"✅ 兼容"判定把模型大小和显存总量比较**，而不是和扣除 KV cache、计算缓冲后的
  实际预算比较。15.9GB 的卡会把 15.3GB 的权重标成"兼容"，而它实际装不下。
  现在按权重预算判定，并区分三种状态：可完整驻留 / 超出多少（部分层落 CPU）/ 远超预算。

- **16GB 显卡被当成 8GB 档**。GPU 指南用 `(vram_mb/1024).toFixed(1) >= 16` 判档，
  而显卡实际上报 16311MiB＝15.9GB，这个条件**永远不成立**。改为直接用 MB 判档。

- **`BF16` 文件被标成 `F16`**。量化类型按"首个子串命中即停"匹配，而 `F16` 排在
  `BF16` 前面，`"bf16"` 里含有 `"f16"`。改为按名字长度从长到短匹配。

- **多种量化档位显示为 "unknown"**：`Q3_K_XL`、`Q4_K_XL`、`Q8_K_XL`、`IQ3_S`、
  `IQ2_S`、`MXFP4_MOE` 等都不在识别列表里。同一份识别逻辑在代码里有
  **4 份互不一致的副本**，其中本地模型库那份连 `IQ4_XS` 都没有。
  现已统一为单个 `detect_quant()`，覆盖 llama.cpp 支持的全部类型及 Unsloth UD 变体。

- **视觉模块显示主模型的量化**。配对命名会把主模型量化写进文件名
  （`...-Q4_K_M-mmproj-BF16.gguf`），现在只取 `mmproj` 之后的部分。

- **设备建议自相矛盾**。`get_recommendations()` 在显存 ≥6GB 时一律提示
  "显存充足，可使用 Q5_K_M 或 Q8_0 获得更好质量"，却完全不看模型多大 ——
  27B 的 Q5_K_M 是 19GB。同时 `推荐 ctx` 只根据**系统内存**计算（固定 2048），
  与真正的约束（显存）无关。现在两者都基于显存预算。

- **`gpu_layers=-1` 的说明是错的**：提示写"将全部层卸载到 GPU"，而它实际是
  按实时显存自动拟合层数；模型装不下时根本做不到全部卸载。

### 新增

- `manager.gpu_weight_budget_gb()` —— 权重能完整驻留显存的体积上限。
  按 1.2.0 的实测数据校准（16GB 卡实测预算 13.45GB）。
- `manager.detect_quant()` —— 统一的量化类型识别。
- 量化提示表补全所有类型，并以实测困惑度替换原先的主观描述
  （如 `IQ4_XS` 原写"比 Q4_K_S 小10%"，未提质量；现为"体积比 Q4_K_M 小约 12%，
  质量几乎持平（+3.1%）；显存吃紧时的首选"）。

### 测试

新增 11 个用例，覆盖量化识别（BF16/UD 变体/mmproj/前缀歧义）、显存预算边界
（必须判定 Q4_K_M 装不下而 IQ4_XS 装得下）、以及建议不再自相矛盾。共 37 passed。

内嵌前端 JS 现经 `node --check` 语法校验。

---

## [1.2.0] - 2026-09-03

围绕"显存装不下模型"这一场景的性能优化。在 RTX 5060 Ti 16GB + Qwen3.8-27B
（`qwen35` 混合注意力架构）上，吐字速度 **6.39 → 16.34 token/s（2.56×）**。

完整测量数据、方法论与无效路径记录见 [docs/performance-tuning.md](docs/performance-tuning.md)。

### 新增

- **显存预算诊断**。权重超出可用显存时，llama.cpp 会静默把层卸载到 CPU，
  速度掉数倍却毫无提示。现在启动时直接指出瓶颈，并给出可执行的建议：
  > 权重 15.3GB 超出可用显存 14.6GB，必然有一部分层留在 CPU 上，这是吐字慢的主因；
  > 当前 ctx_size=16384 还要额外占用 288MiB KV cache…若要让权重完整驻留显存，
  > 需换用体积 ≤13.8GB 的量化档位

  权重装得下、但上下文过长时，则给出当前显存下建议的 `ctx_size` 上限。

- `run.kv_cache_mb()` —— 按模型元数据估算 KV cache 显存占用。
  正确处理**混合注意力架构**：`full_attention_interval=4` 的模型每 4 层才有 1 层
  带 KV，其余是常数大小的 SSM 循环状态。按全层计算会把 KV **高估 4 倍**，
  进而误判上下文预算。

- `run.max_ctx_for_vram()` —— 反解当前显存下权重仍能完整驻留的最大上下文长度。

### 变更

- `gguf_meta()` 新增读取注意力相关字段（`head_count_kv` / `key_length` /
  `value_length` / `full_attention_interval` / `nextn_predict_layers`），
  改为按后缀表统一提取。此前只读 `block_count` 与 `context_length`，
  无法估算 KV cache。

- 27B / 16GB 高压档的 `--fit-target` 由 256 MiB 收紧到 64 MiB（`maximum` 档）。
  权重比显存大时，留给驱动的余量每多一点就少一点权重驻留 GPU；
  实测提速约 15%。`auto` 档保持 256 MiB 的保守值。

### 文档

- 新增 [docs/performance-tuning.md](docs/performance-tuning.md)：完整调优日志，
  含各档位实测数据、量化档位质量对比（困惑度实测）、复现方法，
  以及**已验证无效的优化路径**（见下）。

### 已验证无效（不要重复尝试）

这些手段看起来合理，实测均为负收益，细节见性能文档第 4 节：

| 手段 | 结果 |
|---|---|
| `-ot token_embd.weight=CPU` | 12.91 → **8.41** t/s，会干扰 `--fit` 的张量级自动放置 |
| `--spec-type draft-mtp` | 15.98 → **11.90** t/s，多占约 350MiB 显存把权重挤到 CPU |
| `--spec-type ngram-*` | +2%~6%，在噪声内 |
| 手工指定 `-ngl` | 均劣于 `-ngl auto --fit on` |

> 调优过程中曾单次测到 21.27 t/s（+45%），中位数复测后未能复现，属异常值。
> 本机单次测量波动可达 ±30%，所有结论均改用 3 次取中位数。

### 测试

新增 4 个用例，覆盖 KV 估算（含混合注意力与元数据缺失）与两类显存预警分支。

---

## [1.1.1] 及更早

见 git 提交历史。
